import java.util.Scanner;

class flqone{
    public static void main (String []args){

     // assign the count value in outerloop
    int count=0;

    // for loop statement
    //Syntax for(intial;condision;increment(or)Decrement)
    for(int i=1;i<=10;i++){

        // find the even number 
    if(i%2==0){

        // find the even number count
        count=count+1;
    } 
    }
    
    // assign the sysout in outerloop
    System.out.println(count); 
    }
}